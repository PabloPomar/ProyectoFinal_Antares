import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    tabViewItems: {
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
      width: '100%',
      backgroundColor: 'rgb(53,53,90)'
    },
    tabItemTitle: {
      fontSize: 12,
      color: "black",
    },
  });